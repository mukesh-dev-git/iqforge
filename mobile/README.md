# mobile/

Not created yet. Open Android Studio → New Project → Empty Activity (Compose),
package name `com.iqforge`, save it as this folder (`iqforge/mobile/`).

Once it exists, move everything from `../salvaged/` in per its README, delete this file.
